export const APP_CONFIG = {
    // Fallback URL if env is missing (optional, but good for local dev if not using .env)
    DEFAULT_BASE_URL: 'https://frontdesk.getomnify.ai',

    // Default SecureStore key for email caching
    DEFAULT_EMAIL_CACHE_KEY: 'user_email',

    // UI Constants
    SPLASH_LOADER_COLOR: '#000',
    ERROR_TEXT_COLOR: '#ff4444',
};
